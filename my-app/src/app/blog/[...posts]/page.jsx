//Giving the example of cache all route ( by using [...posts]) it involves dynamic routing also 
// we can also make it optional by using [[...posts]]

const Post = ({params}) => {
  // console.log(params);
  return (
    <div>Post</div>
  )
}

export default Post;